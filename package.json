{
  "name": "video-stream-poc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "concurrently 'cd server && npm run dev' 'cd client && npm run dev'"
  },
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
